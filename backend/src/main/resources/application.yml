server:
  port: ${PORT:8080}

spring:
  datasource:
    # Đã thêm ?prepareThreshold=0 ở cuối
    url: jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?prepareThreshold=0
    username: postgres.lqtsgmqstimwmqlrfbfu
    password: "LgBDN&M*.5dpx@4"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect